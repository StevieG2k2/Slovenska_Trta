@model IEnumerable<web.Models.Trte>

@{
    ViewData["Title"] = "Index";
}

<h1>Trte</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sorta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sorta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
