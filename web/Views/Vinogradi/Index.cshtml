@model IEnumerable<web.Models.Vinogradi>

@{
    ViewData["Title"] = "Index";
}

<h1>Vinogradi</h1>

<p>
    <a asp-action="Create">Ustvari novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                    <a asp-action="Index" asp-route-sortOrder="@ViewData["Povrsina"]">@Html.DisplayNameFor(model => model.Povrsina)</a>
            </th>
            <th>
                    <a asp-action="Index" asp-route-sortOrder="@ViewData["StSort"]">@Html.DisplayNameFor(model => model.StTrt)</a>
            </th>
            <th>
                    <a asp-action="Index" asp-route-sortOrder="@ViewData["Leto"]">@Html.DisplayNameFor(model => model.letoMeritve)</a>
            </th>
            <th>
                    <a asp-action="Index" asp-route-sortOrder="@ViewData["TrteId"]">@Html.DisplayNameFor(model => model.TrteId)</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Povrsina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StTrt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.letoMeritve)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trte.TrteId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VinogradiId">Uredi</a> |
                <a asp-action="Details" asp-route-id="@item.VinogradiId">Podrobnosti</a> |
                <a asp-action="Delete" asp-route-id="@item.VinogradiId">Izbriši</a>
            </td>
        </tr>
}
    </tbody>
</table>
